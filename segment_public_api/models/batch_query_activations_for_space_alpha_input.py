# coding: utf-8

"""
    Segment Public API

    The Segment Public API helps you manage your Segment Workspaces and its resources. You can use the API to perform CRUD (create, read, update, delete) operations at no extra charge. This includes working with resources such as Sources, Destinations, Warehouses, Tracking Plans, and the Segment Destinations and Sources Catalogs.  All CRUD endpoints in the API follow REST conventions and use standard HTTP methods. Different URL endpoints represent different resources in a Workspace.  See the next sections for more information on how to use the Segment Public API. 

    The version of the OpenAPI document: 73.3.1
    Contact: friends@segment.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from segment_public_api.models.pagination_input import PaginationInput
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BatchQueryActivationsForSpaceAlphaInput(BaseModel):
    """
    Input to list activations in bulk across many audiences in one call (space-level read). Intended for callers pulling activation metadata for many audiences on a recurring schedule, to avoid one request per audience against the per-space rate limit.
    """ # noqa: E501
    audience_ids: Optional[List[StrictStr]] = Field(default=None, description="Audience IDs to list activations for. Max 100 IDs per call. Duplicates are de-duplicated server-side but still count toward the cap.  Omit (or pass an empty array) to scan every audience in the space instead - see the `pagination` doc below for how that changes what a \"page\" means. Omitting this list is materially higher latency per call than a scoped batch, since each page has to enumerate audiences before it can look up their activations.", alias="audienceIds")
    pagination: Optional[PaginationInput] = Field(default=None, description="Optional pagination.  When `audienceIds` is provided, this paginates the combined activation result set across those audiences (`count` = activations per page).  When `audienceIds` is omitted, this paginates the space's *audiences* instead, fully expanding each page's activations (`count` = audiences per page, not activations per page) - so response size per page is not directly bounded by `count` in that mode.")
    __properties: ClassVar[List[str]] = ["audienceIds", "pagination"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BatchQueryActivationsForSpaceAlphaInput from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of pagination
        if self.pagination:
            _dict['pagination'] = self.pagination.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BatchQueryActivationsForSpaceAlphaInput from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "audienceIds": obj.get("audienceIds"),
            "pagination": PaginationInput.from_dict(obj["pagination"]) if obj.get("pagination") is not None else None
        })
        return _obj


